# MineWiki 内容投稿指南

感谢你愿意帮助改进 MineWiki。这里的目标不是堆积尽可能多的页面，而是让读者在真实的开服、配置、联机、维护或排错场景中，能看懂、能执行、也能在出问题时安全回退。

> **核心要求：准确、可复现、说清边界。** 如果某个结论依赖特定 Minecraft 版本、加载器、服务端实现、操作系统或网络环境，请把前提写出来；无法核验的猜测不要写成确定结论。

## 适合投稿的内容

| 类型 | 例子 | 投稿重点 |
| --- | --- | --- |
| 新教程 | 域名与 SRV、白名单、备份恢复、权限组、模组服部署 | 按真实操作顺序写清准备、执行、检查与回退。 |
| 既有内容修订 | Java 版本要求、配置项默认值、已失效下载链接 | 提供可靠来源，并说明原内容哪里过时或不准确。 |
| 故障排查 | 无法启动、端口不可达、玩家进服报错、模组不兼容 | 给出症状、常见原因、确认方法与安全的解决步骤。 |
| 工具或界面改进 | 启动脚本、配置生成器、文档导航与可访问性 | 说明目标用户、输入输出、边界、验证方式与回滚影响。 |

以下内容通常不适合直接合入：只有下载链接而没有操作说明的页面、无法复现的“玄学优化”、未经许可的大段转载、包含账号密码或私有地址的示例、绕过正版验证或安全防护的做法，以及把个别环境的体验泛化为所有服务器的结论。

## 开始前：先确认是否值得新建页面

请先浏览网站和仓库中的 `content/docs/`。如果已有接近主题，优先补充、修订或增加一个明确的小节；只有当读者会按独立路径阅读、需要独立的前提与排错逻辑时，才新建页面。对范围较大、涉及多个服务端生态或存在方案分歧的主题，建议先开 Issue 说明选题、适用版本、资料来源和拟定目录，再开始长文撰写。

选择目录时，以读者的任务为准，而不是软件名：首次开服放在 `general/`，网络与代理放在 `network/`，常规配置放在 `config/`，插件生态放在 `plugins/`，加载器与模组放在 `mods/`，持续运行和恢复放在 `maintenance/`，短而明确的问题放在 `faq/`。

## 提交流程

| 步骤 | 要做什么 | 结果 |
| --- | --- | --- |
| 1. 准备主题 | 检索现有页面和 Issue；必要时先讨论范围。 | 不重复、不冲突，目标读者明确。 |
| 2. 创建分支 | 从最新 `master` 新建分支，例如 `docs/domain-srv-guide`。 | 每个 Pull Request 聚焦一类改动。 |
| 3. 编辑内容 | 新增或修订 MDX，并更新相应 `meta.json`。 | 页面能被导航发现，顺序符合学习路径。 |
| 4. 本地验证 | 运行格式、类型和生产构建检查；必要时在浏览器预览。 | 没有解析、链接、类型或构建错误。 |
| 5. 发起 PR | 按模板说明背景、来源、验证与风险。 | 审核者能快速理解和复核改动。 |

可参考以下命令：

```bash
git clone https://github.com/MineHelper/MineWiki.git
cd MineWiki
npm ci
git switch master
git pull --ff-only origin master
git switch -c docs/your-topic

# 完成修改后
npm run lint
npm run types:check
npm run build
```

提交信息使用英文 [Conventional Commits](https://www.conventionalcommits.org/) 格式。内容页面优先使用 `docs(<scope>): <summary>`，例如：

```text
docs(network): add custom domain and SRV guide
fix(config): correct resource pack hash instructions
feat(tools): add startup command preview
```

不要把无关的格式化、依赖升级、个人编辑器配置和新教程混在同一个 Pull Request 中。若必须分阶段完成，请在 PR 描述中说明先后顺序与依赖关系。

## 新建教程的格式

教程保存在 `content/docs/<section>/`，扩展名为 `.mdx`。文件名使用小写英文与连字符，例如 `domain-and-srv.mdx`。每篇页面必须包含 `title` 和 `description` 两项 frontmatter；框架会根据 `title` 生成页面 H1，因此正文**不要再写 `# 一级标题`**，从 `##` 开始组织内容。

```mdx
---
title: 自定义域名与 SRV 记录
description: 让玩家使用易记地址进入 Java 版服务器，并理解记录生效前需要确认的事项
---

先说明读者能解决什么问题、适用什么环境，以及哪些前提尚未满足。

## 准备条件

说明 Minecraft 版本、服务端类型、域名控制权、端口或网络前提。

## 配置步骤

1. 写清操作位置和输入内容。
2. 解释关键字段的作用与常见错误。
3. 给出安全的验证方法。

## 常见问题

说明症状、如何确认原因，以及恢复或回滚方式。

## 参考

[1]: https://example.com "来源名称"
```

新增页面后，必须将不含扩展名的文件名加入对应目录的 `meta.json`。顺序应符合读者的学习路径，而不是按文件名或提交时间排列。若新主题开启了一个新的顶级章节，还需要更新 `content/docs/meta.json`。

## 写作与技术事实要求

正文应使用自然、克制的中文，先说明读者的目标和前提，再给出操作和验证方法。尽量用完整句子解释“为什么这样做”，但不要把简单命令写成冗长叙述。面向新手时，首次出现的术语应给出简短解释；面向特定服务端生态时，必须明确 Paper、Vanilla、Fabric、Forge、NeoForge 或代理软件等适用范围。

| 需要做到 | 具体要求 |
| --- | --- |
| 事实可核验 | 版本要求、兼容性、配置键、默认行为、安全限制和迁移步骤优先引用官方或一手文档，例如 Paper、Fabric、NeoForge、Minecraft Wiki、Mojang 或软件项目自身文档。 |
| 来源可访问 | 在文末 `## 参考` 中使用引用式链接。链接文本应说明来源与页面主题，不使用“点击这里”或裸链接。 |
| 边界清楚 | 写明适用的版本、平台、网络环境和前置条件；对于会随版本变化的信息，说明检查时间或引导读者查看上游版本页。 |
| 操作可复现 | 命令标明运行环境与目录；配置示例保留必要上下文；步骤之后给出观察成功的方式。 |
| 风险可回退 | 涉及世界、玩家数据、权限、端口、代理或升级时，先说明备份、停服窗口、测试副本和恢复方法。 |
| 语气专业 | 避免贬损、夸张、绝对化或无法验证的排名。使用“通常”“在此条件下”“请先确认”等有边界的表达。 |

引用只用于支撑事实，不替代自己的解释。不要直接复制其他网站的大段文字、截图或表格；需要引用少量原文时，请控制篇幅、标明出处，并确认其许可允许使用。涉及个人信息时，使用 `example.com`、`203.0.113.0` 等示例值，绝不要提交真实 IP、域名、令牌、密码、密钥、玩家 UUID、日志中的隐私内容或内部管理地址。

## 命令、配置与代码块

所有多行命令与配置使用带语言标识的 fenced code block。命令示例应尽量提供可复制的最小版本，并在上下文中说明其含义、运行目录和风险。不要为了“看起来完整”而加入未验证的 JVM 参数、模组、下载 URL 或命令别名。

```bash
# 在服务端根目录执行；修改前请先备份。
java -Xms2G -Xmx2G -jar paper.jar --nogui
```

当配置值会影响数据安全、权限、开放网络访问或玩家连接时，紧接着写明：默认行为是什么、变更后会影响谁、如何验证、如何撤回。对于资源包 URL、反向代理、端口转发和 Web 服务等例子，不得包含访问令牌或仅在作者私有环境中可用的地址。

## 链接、导航与 MDX 组件

站内链接使用站点路径，例如 `[首次开服](/docs/java/intro/setup)`，不要使用本地开发地址。提交前逐一检查新增或修改的链接，避免锚点、文件名和目录不一致。外部链接应使用 HTTPS，并优先指向官方文档或项目发布页。

可使用仓库已注册的 MDX 组件来改善信息层级，但组件不是内容质量的替代品。首次投稿优先使用普通 Markdown；如需 `Callout`、步骤、文件树或快捷链接组件，请先参考同类现有页面，确认属性与用途一致。不要新增未经注册的组件标签，也不要用组件隐藏关键前提或风险提示。

## 提交前检查清单

在发起 Pull Request 前，请确认以下事项：

- [ ] 主题与现有内容不重复，或已说明本次补充的差异。
- [ ] frontmatter 包含准确的 `title` 与 `description`，正文没有重复 H1。
- [ ] 文件位于正确章节，且所有相关 `meta.json` 已更新。
- [ ] 标题层级连续，表格、列表、引用和代码围栏完整闭合。
- [ ] 站内链接可访问，外部技术结论有可靠来源，参考链接位于文末。
- [ ] 所有版本、兼容性和安全结论都写清适用前提；高风险操作已包含备份或回退说明。
- [ ] 示例不包含私密信息、受版权保护的大段转载或诱导绕过安全措施的内容。
- [ ] 已运行 `npm run lint`、`npm run types:check` 与 `npm run build`。
- [ ] 如修改页面样式、交互或工具，已在浏览器中检查桌面与窄屏显示，并在 PR 中说明结果。

## Pull Request 要求与审核方式

请使用仓库的 Pull Request 模板，如实填写问题背景、改动范围、技术来源、验证命令和风险说明。提交 PR 后请保持改动聚焦，并及时回应可复现的审核意见。维护者可能要求补充来源、缩小结论范围、增加回退步骤、修复导航或拆分无关改动；这些要求是为了让页面在未来版本变化后仍然可信和可维护。

通常，以下问题会阻止合并：页面无法构建、缺少导航登记、技术结论没有可靠来源、缺少关键版本前提、可能导致世界或权限风险却未提示备份与回退、示例泄露敏感信息、复制内容的版权不清晰，或修改范围与 PR 描述明显不一致。

如果你不确定某项操作是否安全、某个版本是否兼容，最好的做法是在 PR 中明确提出疑问、给出已查阅的资料，并等待讨论；这比提交看似完整但无法验证的教程更有价值。

## 获取帮助

内容选题、目录归属或技术事实存在疑问时，请先在 [Issues](https://github.com/MineHelper/MineWiki/issues) 中说明你的场景、目标版本、已尝试操作和查阅过的来源。这样维护者与其他贡献者可以更有效地帮助你确定下一步。
